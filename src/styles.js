import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
    headerContainer: {
        flex: 0.5,
        backgroundColor: '#fff',
        alignItems: 'top',
        justifyContent: 'top',
      },
    headers: {
        fontSize: 30,
        color: 'Black',
    },
  });